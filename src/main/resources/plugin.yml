name: ${project.artifactId}
version: ${project.version}
main: ${project.groupId}.${project.artifactId}
author: LoreSchaeffer
description: Add support to NBT in the crafting table
api-version: 1.19
website: https://github.com/LoreSchaeffer/NBTRecipes
prefix: NBTR

commands:
  nbtrecipes:
    aliases: [ "nbtr" ]